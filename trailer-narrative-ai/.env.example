# Trailer Narrative AI - Environment Configuration
# All features run locally - NO paid API keys required!

# AWS Configuration (Optional - only for S3 storage)
AWS_S3_BUCKET=stage-media-bucket
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key

# LLM Configuration
# Default: local (uses Hugging Face Transformers - auto-downloads, FREE)
# Other options: none (heuristic only), ollama, anthropic
LLM_PROVIDER=local

# For local LLM (default - recommended, FREE, auto-downloads)
# Model options: google/flan-t5-base (small/fast), google/flan-t5-large (better quality)
LLM_MODEL=google/flan-t5-base

# For Ollama (optional - uncomment if you want to use Ollama)
# LLM_PROVIDER=ollama
# LLM_MODEL=llama3.2
# OLLAMA_HOST=http://localhost:11434

# For paid Claude API (optional - uncomment if using anthropic)
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-sonnet-4-20250514
# ANTHROPIC_API_KEY=your_anthropic_api_key

# Whisper Configuration (FREE - runs locally)
WHISPER_MODEL=medium
WHISPER_DEVICE=auto

# Visual Analysis Configuration (FREE - CLIP runs locally)
VISUAL_MODEL=clip
VISUAL_DEVICE=auto

# AI Music Generation (FREE - Meta's MusicGen runs locally)
MUSIC_ENABLED=true
MUSICGEN_MODEL=small
MUSICGEN_DEVICE=auto

# Output Configuration
OUTPUT_DIR=./output
